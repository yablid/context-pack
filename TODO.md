
SO i need a context pack option to 1) exclude subdirs like ctxp packages --paste --no browser-runtime browser-injectors > this.txt

and one to include matching globs like:
ctxp packages --paste --yes package.json tsconfig.json > buildstuff.txt